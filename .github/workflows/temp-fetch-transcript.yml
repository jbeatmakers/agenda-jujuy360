name: Temporary Whisper base fetch

on:
  pull_request:
    paths:
      - 'TEMP_TRANSCRIPT_TRIGGER'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch faster-whisper base model
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p faster-whisper-base
          base='https://huggingface.co/Systran/faster-whisper-base/resolve/main'
          for file in config.json model.bin tokenizer.json vocabulary.txt; do
            curl --fail --location --retry 5 --retry-all-errors "$base/$file" --output "faster-whisper-base/$file"
          done
          test -s faster-whisper-base/model.bin
          sha256sum faster-whisper-base/model.bin
          du -sh faster-whisper-base

      - name: Upload model artifact
        uses: actions/upload-artifact@v4
        with:
          name: faster-whisper-base
          path: faster-whisper-base
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
